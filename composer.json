{
    "name": "laramore/uuid-field",
    "description": "Add an UUID field for Laramore",
    "type": "library",
    "autoload": {
        "psr-4": {
            "Laramore\\": "src/"
        }
    },
    "require": {
        "ramsey/uuid": ">=3.8",
        "laramore/laramore": "^0.5",
        "laramore/grammar-types": "^0.1.0"
    },
    "conflict": {
        "laravel/framework": "< 5.6"
    },
    "license": "MIT",
    "authors": [
        {
            "name": "NastuzziSamy",
            "email": "samy@nastuzzi.fr"
        }
    ],
    "extra": {
        "laravel": {
            "providers": [
                "Laramore\\Providers\\UuidProvider"
            ]
        }
    },
    "scripts": {
        "test:lint": [
            "./vendor/bin/phpcs src",
            "./vendor/bin/phpmd src text phpmd.xml"
        ],
        "fix:lint": [
            "./vendor/bin/phpcbf src"
        ],
        "test:unit": [
            "./vendor/bin/phpunit --bootstrap vendor/autoload.php tests"
        ],
        "test": [
            "@composer test:lint",
            "@composer test:unit"
        ]
    },
    "require-dev": {
        "phpmd/phpmd": ">=2.7",
        "squizlabs/php_codesniffer": ">=3.5",
        "phpstan/phpstan": ">=0.9.2",
        "phpunit/phpunit": ">=6.5"
    },
    "suggest": {
        "laramore/migrations": "Generate automatically all your migrations"
    }
}
